const handleRegister = require('./handleRegister');
const handleLogin = require('./handleLogin');
const handleCats = require('./handleCats');
const handleRandom = require('./handleRandom');

module.exports = {
  handleRegister,
  handleLogin,
  handleCats,
  handleRandom,
};
